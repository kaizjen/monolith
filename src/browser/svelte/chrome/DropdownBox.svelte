<style>
  .box {
    padding-block: 8px;
    padding-inline: 12px;
    background: var(--active-background);
    color: var(--accent-text);
    display: flex;
    align-items: center;
    padding-left: 22px;
  }
</style>

<div class="box">
  <slot />
</div>